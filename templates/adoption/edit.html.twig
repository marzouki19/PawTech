{% extends 'base.html.twig' %}

{% block title %}Edit Adoption{% endblock %}

{% block body %}
    <h1>Edit Adoption</h1>

    {{ include('adoption/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_adoption_index') }}">back to list</a>

    {{ include('adoption/_delete_form.html.twig') }}
{% endblock %}

